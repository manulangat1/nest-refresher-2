pipeline { 
    agent any 

    stages {
        stage("init") {
            steps{ 
                sh ("Echo Hello world")
            }
            
        }
        stage( "Build") {
            echo "Hello world"
        }
    }
}